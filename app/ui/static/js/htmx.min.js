/*! Harmony-local HTMX compatibility shim */
(function (global) {
  if (global.htmx) {
    return;
  }
  function warn(method) {
    if (typeof console !== "undefined" && console.warn) {
      console.warn("HTMX shim invoked: '" + method + "' is not implemented.");
    }
  }
  global.htmx = {
    version: "0.0.0-local",
    trigger: function trigger() {
      warn("trigger");
      return null;
    },
    ajax: function ajax() {
      warn("ajax");
      return Promise.resolve();
    },
    onLoad: function onLoad(callback) {
      if (typeof callback === "function") {
        callback(document.body);
      }
    },
  };
})(typeof window !== "undefined" ? window : globalThis);
