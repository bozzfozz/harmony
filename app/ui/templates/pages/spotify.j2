{% extends "layouts/base.j2" %}
{% import "partials/_strings.j2" as strings %}

{% block header_actions %}
  <form id="logout-form" method="post" action="/ui/logout">
    <input type="hidden" name="csrftoken" value="{{ csrf_token }}" />
    <button type="submit">{{ strings.button_label("logout") }}</button>
  </form>
{% endblock %}

{% block page_header %}
  <section>
    <h2>{{ strings.section_heading(layout.page_id) }}</h2>
    <p>{{ strings.spotify_page_intro() }}</p>
  </section>
{% endblock %}

{% block main %}
  <section class="page-section page-section--card" aria-labelledby="{{ status_fragment.identifier }}-heading">
    <h3 id="{{ status_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_status_heading() }}</h3>
    <div id="{{ status_fragment.identifier }}"
         class="async-fragment"
         role="status"
         aria-live="polite"
         aria-labelledby="{{ status_fragment.identifier }}-heading"
         data-fragment="spotify-status"
         hx-get="{{ status_fragment.url }}"
         hx-trigger="{{ status_fragment.trigger }}"
         hx-target="{{ status_fragment.target }}"
         hx-swap="{{ status_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(status_fragment.loading_key) }}</p>
    </div>
  </section>
  <section class="page-section page-section--card" aria-labelledby="{{ account_fragment.identifier }}-heading">
    <h3 id="{{ account_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_account_heading() }}</h3>
    <div id="{{ account_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ account_fragment.identifier }}-heading"
         data-fragment="spotify-account"
         hx-get="{{ account_fragment.url }}"
         hx-trigger="{{ account_fragment.trigger }}"
         hx-target="{{ account_fragment.target }}"
         hx-swap="{{ account_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(account_fragment.loading_key) }}</p>
    </div>
  </section>
  <section class="page-section page-section--card" aria-labelledby="{{ top_tracks_fragment.identifier }}-heading">
    <h3 id="{{ top_tracks_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_top_tracks_heading() }}</h3>
    <div id="{{ top_tracks_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ top_tracks_fragment.identifier }}-heading"
         data-fragment="spotify-top-tracks"
         hx-get="{{ top_tracks_fragment.url }}"
         hx-trigger="{{ top_tracks_fragment.trigger }}"
         hx-target="{{ top_tracks_fragment.target }}"
         hx-swap="{{ top_tracks_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(top_tracks_fragment.loading_key) }}</p>
    </div>
  </section>
  <section class="page-section page-section--card" aria-labelledby="{{ top_artists_fragment.identifier }}-heading">
    <h3 id="{{ top_artists_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_top_artists_heading() }}</h3>
    <div id="{{ top_artists_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ top_artists_fragment.identifier }}-heading"
         data-fragment="spotify-top-artists"
         hx-get="{{ top_artists_fragment.url }}"
         hx-trigger="{{ top_artists_fragment.trigger }}"
         hx-target="{{ top_artists_fragment.target }}"
         hx-swap="{{ top_artists_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(top_artists_fragment.loading_key) }}</p>
    </div>
  </section>
  <section class="page-section page-section--card" aria-labelledby="{{ recommendations_fragment.identifier }}-heading">
    <h3 id="{{ recommendations_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_recommendations_heading() }}</h3>
    <p class="page-section__description">{{ strings.spotify_recommendations_description() }}</p>
    <div id="{{ recommendations_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ recommendations_fragment.identifier }}-heading"
         data-fragment="spotify-recommendations"
         hx-get="{{ recommendations_fragment.url }}"
         hx-trigger="{{ recommendations_fragment.trigger }}"
         hx-target="{{ recommendations_fragment.target }}"
         hx-swap="{{ recommendations_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(recommendations_fragment.loading_key) }}</p>
    </div>
  </section>
  <section class="page-section page-section--card" aria-labelledby="{{ saved_fragment.identifier }}-heading">
    <h3 id="{{ saved_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_saved_tracks_heading() }}</h3>
    <div id="{{ saved_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ saved_fragment.identifier }}-heading"
         data-fragment="spotify-saved-tracks"
         hx-get="{{ saved_fragment.url }}"
         hx-trigger="{{ saved_fragment.trigger }}"
         hx-target="{{ saved_fragment.target }}"
         hx-swap="{{ saved_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(saved_fragment.loading_key) }}</p>
    </div>
  </section>
  <section class="page-section page-section--card" aria-labelledby="{{ playlists_fragment.identifier }}-heading">
    <h3 id="{{ playlists_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_playlists_heading() }}</h3>
    <div id="{{ playlists_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ playlists_fragment.identifier }}-heading"
         data-fragment="spotify-playlists"
         hx-get="{{ playlists_fragment.url }}"
         hx-trigger="{{ playlists_fragment.trigger }}"
         hx-target="{{ playlists_fragment.target }}"
         hx-swap="{{ playlists_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(playlists_fragment.loading_key) }}</p>
    </div>
  </section>
  <section class="page-section page-section--card" aria-labelledby="{{ artists_fragment.identifier }}-heading">
    <h3 id="{{ artists_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_artists_heading() }}</h3>
    <div id="{{ artists_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ artists_fragment.identifier }}-heading"
         data-fragment="spotify-artists"
         hx-get="{{ artists_fragment.url }}"
         hx-trigger="{{ artists_fragment.trigger }}"
         hx-target="{{ artists_fragment.target }}"
         hx-swap="{{ artists_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(artists_fragment.loading_key) }}</p>
    </div>
  </section>
  {% if session.features.imports and free_ingest_fragment %}
    <section class="page-section page-section--card" aria-labelledby="{{ free_ingest_fragment.identifier }}-heading">
      <h3 id="{{ free_ingest_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_free_ingest_heading() }}</h3>
      <div id="{{ free_ingest_fragment.identifier }}"
           class="async-fragment"
           role="region"
           aria-live="polite"
           aria-labelledby="{{ free_ingest_fragment.identifier }}-heading"
           data-fragment="spotify-free-ingest"
           hx-get="{{ free_ingest_fragment.url }}"
           hx-trigger="{{ free_ingest_fragment.trigger }}"
           hx-target="{{ free_ingest_fragment.target }}"
           hx-swap="{{ free_ingest_fragment.swap }}">
        <p class="async-fragment__placeholder">{{ strings.fragment_loading(free_ingest_fragment.loading_key) }}</p>
      </div>
    </section>
  {% endif %}
  <section class="page-section page-section--card" aria-labelledby="{{ backfill_fragment.identifier }}-heading">
    <h3 id="{{ backfill_fragment.identifier }}-heading" class="page-section__title">{{ strings.spotify_backfill_heading() }}</h3>
    <div id="{{ backfill_fragment.identifier }}"
         class="async-fragment"
         role="region"
         aria-live="polite"
         aria-labelledby="{{ backfill_fragment.identifier }}-heading"
         data-fragment="spotify-backfill"
         hx-get="{{ backfill_fragment.url }}"
         hx-trigger="{{ backfill_fragment.trigger }}"
         hx-target="{{ backfill_fragment.target }}"
         hx-swap="{{ backfill_fragment.swap }}">
      <p class="async-fragment__placeholder">{{ strings.fragment_loading(backfill_fragment.loading_key) }}</p>
    </div>
  </section>
{% endblock %}
