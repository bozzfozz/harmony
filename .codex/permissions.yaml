defaults:
  mode: implement
  write_paths:
    - "**"
  deny_paths:
    - "LICENSE"
    - "BACKUP/**"

fast_track:
  prefixes:
    - "CODX-ORCH-"
    - "CODX-OPS-"
  effect:
    mode: implement
    ignore_session_readonly: true
fallback:
  patches_dir: "patches"
