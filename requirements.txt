# Runtime dependencies for Harmony backend
# FastAPI 0.115.11 lifts the Starlette <0.47.0 constraint, but 0.47.x currently
# regresses FastAPI routing. Hold Starlette below 0.47.0 until an upstream fix
# lands, even though this temporarily drops the GHSA-2c2j-9gv5-cj73 mitigation.
# Re-upgrade as soon as FastAPI publishes a compatible release.

fastapi==0.115.11
starlette<0.47.0
uvicorn==0.30.1
sqlalchemy==2.0.31
aiohttp==3.12.14
aiosqlite==0.19.0
spotipy==2.25.1
pydantic==2.7.1
httpx==0.27.0
psutil==5.9.8
mutagen==1.47.0
prometheus-client==0.20.0
Unidecode==1.3.8
